# Python API Client Project

## Setup
1. Copy `.env.example` to `.env` and fill in your API key and signature.
2. Install dependencies: `pip install -r requirements.txt`

## Usage
See `src/api_client.py` for example usage.
